# Bind the panel touchscreen to the DSI-1 weston output and rotate its axes to
# match the output transform (rotate-90). Validated on hardware 2026-07-17.
SUBSYSTEM=="input", ATTRS{name}=="Goodix Capacitive TouchScreen", ENV{WL_OUTPUT}="DSI-1", ENV{LIBINPUT_CALIBRATION_MATRIX}="0 1 0 -1 0 1"
