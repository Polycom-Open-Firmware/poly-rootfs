# / is mounted by the initramfs: read-only lower + tmpfs overlay by default,
# or direct-rw when maintenance mode is armed (see tc8-rw/tc8-ro/tc8-mode).
# facres -> /persist (/root) is handled by tc8-persist-root.service.
proc             /proc    proc    defaults                          0 0
sys              /sys     sysfs   defaults                          0 0
tmpfs            /tmp     tmpfs   defaults,nosuid,nodev,size=64M    0 0
tmpfs            /run     tmpfs   defaults,nosuid,nodev,mode=755    0 0
tmpfs            /var/log tmpfs   defaults,nosuid,nodev,size=32M    0 0
